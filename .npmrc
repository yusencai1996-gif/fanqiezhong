# 项目级 npm 配置
# electron 二进制走 npmmirror 镜像,绕过 github.com 下载被墙(ECONNRESET/SSL错误)
# electron 官方 install 脚本读 ELECTRON_MIRROR 环境变量(不是 npm config key)
# npm 的 .npmrc 用 "包名:变量" 语法设置 per-package env
electron:ELECTRON_MIRROR=https://cdn.npmmirror.com/binaries/electron/

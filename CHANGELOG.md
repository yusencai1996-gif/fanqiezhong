# 更新日志 (CHANGELOG)

> 番茄钟桌面应用版本历史。遵循 [SemVer 语义化版本](https://semver.org/lang/zh-CN/):
> - 主版本(第1位):重大里程碑/不兼容改动
> - 次版本(第2位):**新增功能**(向后兼容)
> - 修订号(第3位):**修复/小改进**(向后兼容)

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/),按版本倒序(最新在上)。

---

## [v0.5.1] - 2026-09-24 (修复·模型 ID 更新为官方最新)

> 🔧 用户实测评议发现模型选项停留在已退役的 `deepseek-chat` / `deepseek-reasoner`。按 DeepSeek 官方文档更新为当前有效 ID:**`deepseek-flash`**(V4.1-Flash,默认,支持思考/非思考模式)与 **`deepseek-v4-pro`**(V4-Pro 旗舰)。

### 修复
- 模型选择列表与后端校验集合更新为 `deepseek-flash` / `deepseek-v4-pro`(全局 40 处)
- 存量配置中的旧模型 ID 自动回退默认模型,无需数据迁移

### 测试
- 278 测试全绿(模型名随更新,用例数不变)

---

## [v0.5.0] - 2026-09-24 (AI 陪学助理第一期 + 数据安全加固)

> 🤖 接入 DeepSeek(可选):设置中填入自己的 API 密钥即可对话、今日复盘、获取今日学习顺序建议。AI 全程只读不改数据;不配置密钥则应用行为与 v0.4.2 完全一致。经七关流程开发(双线实施 + 三路审核 + 独立复审)。

### 新增·AI 陪学助理(第一期,全部只读)
- **设置**:DeepSeek API 密钥(本地存储、界面只显示掩码)、模型选择(连接测试通过后可选最新模型)、连接测试
- **对话面板**:顶栏助手入口(宽窄两套),多轮对话基于真实学习数据回答
- **今日复盘**:当日+近 7 天数据分析(完成情况/趋势对比/计划点评/明日建议)
- **今日学习顺序建议**:按当日计划与近期投入给"先学 X 约 N 分钟"式建议,可追问调整
- **安全设计**:密钥仅存于主进程与本地数据文件,渲染层只拿掩码;AI 无任何写数据通道;请求超时/取消/错误码全映射为固定中文文案

### 修复·幽灵计划(v0.4.2 审核遗留)
- 恢复目标时,名下科目中的计划(含曾被单独归档的)一并恢复归档标记,消除"恢复目标后计划永久隐藏"的可达序列

### 加固·数据与网络(两轮审核阻断/建议项整改)
- **数据文件损坏自愈**:解析失败时自动将原文件改名备份(`data.json.corrupt-时间戳`)并以全新状态启动,消除"损坏后静默拒绝一切写入、新数据全丢"的风险
- **原子写入**:先写临时文件再替换,消除掉电/强杀留下 0 字节数据文件
- 取消请求立即释放占用(切换模型后立刻发新请求不再误报"进行中")
- 短于 12 位的密钥掩码不再显示尾四位;400/404/422 类错误不再引导重试;超大响应按 content-length 预检拒收
- 落盘失败留下可观测日志;启动加载异常不再卡死界面

### 数据结构
- schemaVersion v9 → v10:`settings.aiConfig`(apiKey/model),旧数据自动迁移,幂等可重放

### 测试
- 278 测试全绿(v0.4.2 的 190 + 新增 88:AI 纯函数/密钥网关/网络服务/IPC 校验/持久化自愈/幽灵计划回归等)

---

## [v0.4.2] - 2026-09-19 (修复·归档目标仍显示在今日计划)

> 🐛 修复用户反馈的"目标归档后,主界面今日计划仍显示其名下计划"——根因是归档目标时科目级联归档但计划未动,今日计划计算只认计划自身归档标记。

### 修复·归档目标的计划漏进今日计划
- **问题**:归档目标后,主界面下方"今日计划"和打卡表的"今日计划进度"仍显示该目标名下的计划(全部标"已超期")。
- **根因**:`archiveGoal` 级联归档科目但不动计划——计划自身 `archived` 保持 false、status 保持"进行中";而 `todayPlanSummary` 只按计划自身标记过滤,不知道所属科目/目标已归档。
- **修复**:`todayPlanSummary` 新增 subjects 参数,排除"科目已归档"(含归档目标级联)和"科目已被彻底删除"的计划;`TodayPlanPanel` / `CheckinPanel` 调用时传入 subjects。不改任何存量数据,已归档目标立即从今日计划消失。
- **兼容**:不传 subjects 的老调用保持原行为(向后兼容)。

### 测试
- 190 测试全绿(v0.4.1 的 186 + 新增 4:归档科目过滤 / 科目已删过滤 / 老调用兼容 / store→今日计划集成回归)。

---

## [v0.4.1] - 2026-07-14 (通知修复·全屏听课不弹提醒)

> 🔔 修复森哥反馈的"全屏听课25分钟番茄钟没弹提醒"——根因是从未设置 AppUserModelID,Windows 静默吞掉通知。

### 修复·通知静默丢失(根因)
- **问题**:专注结束时番茄钟不弹系统通知,全屏听课场景尤其明显(看不到界面变化,只能靠通知)。
- **根因**:`main.js` 从未调用 `app.setAppUserModelId()`。Windows 要求应用必须登记身份才能弹通知,否则 `new Notification().show()` 不报错但被静默吞掉。
- **修复**:在 `app.whenReady()` 之前调用 `app.setAppUserModelId('com.xiaokong.pomodoro')`(值与 package.json appId 一致)。

### 改进·通知失败兜底
- 给 `new Notification()` 加 try-catch + `on('failed')` 事件监听。
- 通知被系统拒绝时,降级为**任务栏图标闪烁 + 托盘 tooltip 提示**——即使通知系统彻底不可用,用户也能看到提醒。
- 窗口获得焦点时自动复位 tooltip + 停止闪烁。

### 加固·计时中改设置导致通知失效(Explore 排查发现的隐患)
- **问题**:专注进行中改时长设置 → timer 实例被重建(新实例 stopped)→ 主进程到点发"时间到"信号 → 渲染层守卫读到新实例 stopped → 误拦截 → 不通知。
- **修复**:running/paused 状态下不重建 timer 实例(新时长下一轮生效)。重建判定改用 `timer.getTotalSec()` 比较实例实际时长(而非外部标记,防预写导致重置后不重建)。
- 顺带修复:`handleFinished`/`handleSkip` 记录专注时长从 `minutes*60`(当前渲染值)改为 `timer.getTotalSec()`(实例实际值),防"专注中改设置"导致记录虚增。

### 测试
- 186 测试全绿(v0.4.0 的 185 + 新增 1:getTotalSec)。

### 审核
- code-reviewer 初筛:发现 1 高危(重置后新时长不生效)+ 1 中危(记录虚增)+ 2 低危(tooltip 复位/show catch),全部修复。
- 小码外部审核通过($0.34,逻辑完备,三个场景验证正确)。

### ⚠️ 系统层因素(代码无法解决)
即便修好代码,以下 Windows 设置仍可能拦截通知,需森哥检查:
1. **Windows 通知权限**:设置 → 系统 → 通知,确认开启且番茄钟在允许列表。
2. **专注助手/勿扰模式**:全屏运行时 Windows 可能自动开启,吞掉所有通知。
3. **通知横幅设置**:确认番茄钟是"显示横幅"而非"仅通知中心"。

---

## [v0.4.0] - 2026-07-02 (数据保真·科目级语义明确化 + 手动覆盖修复)

> 🎯 修复 planDoneHours 多计划共享科目"虚高"的数据语义问题(小码多次识别的中优先遗留项),顺带修了手动覆盖的终态优先级 bug。

### 背景
- **已知问题**:同一科目(如"英语")被多个计划共享时,每个计划的进度条都把该科目总投入算成自己的完成量,造成视觉上的"虚高"。根因是 session(专注记录)只记录 subjectId(科目),不记录 planId(计划),无法精确归属到计划。
- **影响范围**:目标总进度条**不受影响**(v0.3.9 已做 subject 去重);仅"单计划进度条/预测/燃尽图"显示科目级数据。

### 设计决策(方案 C:科目级语义明确化)
- **为什么不加 planId(方案 A)**:历史老数据无法回填(过去几个月的 session 没有 planId),且改变核心交互(开始专注还要选计划),得不偿失。
- **为什么不做按比例分摊(方案 B)**:为低频场景(多计划共享科目)增加全局计算复杂度,且分摊是估算非真实。
- **采用方案 C**:数据本来就是科目级粒度(选任务→任务归属科目),那"计划进度=科目投入"在语义上是对的,只是之前没标清楚。把话说明白,比假装精确更诚实。

### 改进·语义明确化
- **GoalManager 计划行**:进度文案从 `10/20h` 改为 `科目投入 10/20h`;同科目多计划时标题旁标"·共享科目"(琥珀色)。
- **ProgressPanel 完成预测**:检测到共享科目时,顶部显示提示"以下进度按科目统计,各计划的已学是科目总投入,非该计划独占"。
- **ProgressPanel 燃尽图**:标题在共享科目时标"共享科目";图例"你的实际"改为"该科目实际投入";提示语"剩余工作量"改为"该科目剩余投入"。
- **代码注释强化**:planDoneHours / subjectDoneSec / planForecast / burndownData 顶部明确标注"科目级粒度,多计划共享是设计边界非 bug"。

### 修复·手动覆盖终态优先级(planning.js)
- **问题**:`planTodayHours` 里手动覆盖优先级最高,会绕过"已完成/超期"判断——计划都完成了,当天手动设了"学2小时",系统仍显示"今天应学2小时"。
- **修复**:把"已完成 >= 总"和"超期"判断挪到手动覆盖之前。终态计划(已完成/超期)即使有手动覆盖也返回 0。
- **影响**:目前手动覆盖 UI 尚未接入(无界面入口),但逻辑修复避免未来接入时的语义错乱。

### 测试
- 183 测试全绿(v0.3.13 的 179 + 新增 4):
  - planning:同科目多计划 planDoneHours 相同(科目级语义验证)
  - planning:已完成计划+手动覆盖→0(终态优先)
  - planning:超期计划+手动覆盖→0(终态优先)
  - forecast:同科目多计划 planForecast.doneHours 相同(科目级语义验证)

### 不做的事(明确边界)
- ❌ 不给 session 加 planId(老数据回填无解)
- ❌ 不做按比例分摊(为低频场景增加全局复杂度)
- ❌ 不改目标总进度逻辑(它已正确)

---

## [v0.3.16] - 2026-07-02 (待办清单纵向布局 + 窗口默认放大)

> 📐 解决待办清单横向拥挤、长标题被功能按钮挤压换行的问题;同时放大默认窗口。

### 改进
- **待办清单纵向两行布局**:任务项由横向 `[☑][标题][5按钮]` 改为纵向——
  - **顶行**:checkbox + 标题(独占整行,`word-break` 自动换行) + 活跃点
  - **底行**:标签(左对齐) + 功能按钮(右对齐,hover 才显示)
  - 标签为空时按钮也靠右占位,布局始终稳定。
- **窗口默认尺寸** 960×640 → **1200×798**:配合 v0.3.15 的 clamp 字号缩放,默认即放大到上限,首屏更舒服。

### 技术细节
- `TaskItem.jsx`:从 `task-item__body`(横向嵌套)重构为 `task-item__row--top/--bottom`(纵向两行);标签输入/科目选择移至最下方独立行,不参与左右挤压。
- `TaskList.css`:`.task-item` 改 `flex-direction: column`;底行 `padding-left: 24px` 与 checkbox 右侧对齐。

---

## [v0.3.15] - 2026-07-01 (UI 随窗口自适应缩放 + 窄屏顶栏折叠)

> 🔍 全局字号/UI 随窗口尺寸平滑缩放,窄屏下顶栏 6 按钮自动折叠成菜单。

### 改进
- **字号 clamp 自适应**:全局字号 token 改用 `clamp(最小, vw+vh 混合, 最大)` 驱动——小窗口不挤、大窗口不空。基准校准:960×640 时各值≈原固定值。
- **顶栏图标/按钮随窗口缩放**:`.app__icon-btn`、分隔线、lucide 图标尺寸均改为 clamp。
- **窄屏顶栏折叠**(`App.jsx`):窗口宽度 <820px 时,6 个功能按钮折叠成 ☰ 菜单 + 下拉,防止挤占计时区。

### 技术细节
- `--font-size-sm/md/lg/xl`、`.app__icon-btn` 宽高、svg 尺寸、`.app__topbar-sep` 高度全部 clamp 化。
- `App.jsx` 监听 resize,`narrow` 状态切换两套顶栏 DOM;下拉用透明 overlay 实现点击外部关闭。
- body 顶部 padding 增加 44px 安全区,防计时区内容与悬浮顶栏重叠。

---

## [v0.3.13] - 2026-06-29 (目标倒计时)

> ⏰ 主界面常驻显示距目标deadline还有多少天,让时间压力可视化。

### 新增
- **目标倒计时行**:主界面 Timer 下方常驻一行,显示距最紧迫的目标 deadline 还有多少天。例:`距 🎯中级会计考试 还有 65 天`。
- **过期警示**:目标已过期 → 红字"🎯 xxx 已超期 X 天" + danger 色边框。
- **今天截止强调**:今天到期 → 主色"今天截止"。
- **点击跳转**:点倒计时行 → 打开进度预测面板(看各计划 deadline 详情)。
- **智能取目标**:多个目标时取最紧迫的(有 deadline 的未归档目标,按截止日升序取第一个)。无 deadline 的目标不显示。

### 技术细节
- 新增 `daysUntil(deadline, now)` 纯函数(date.js):deadline当天23:59减now向上取整。
- 子agent自审修了1个 bug:isToday判定(原`days===0`几乎永不成立且会误标刚过期目标,改成`days===1`)。
- 179 测试绿(新增7个 daysUntil 测试)。
- 已知限制(后续优化):应用空闲跨午夜时倒计时不自动刷新(需等下次交互/store变更),和TodaySummary一致的既有模式。

---

## [v0.3.12] - 2026-06-26 (任务选中持久化)

> 🎯 选了任务就一直按它计时,关掉重开也不丢。

### 修复·任务选中状态丢失(设计缺陷)
- **问题**:用户选了任务专注,关掉番茄钟重开后,下一轮变成自由专注(任务丢失)。森哥反馈:"我以为下个番茄钟还是这个任务,结果是自由专注?这设计不合理吧"。
- **根因**:activeTaskId 是纯内存 useState,应用重启即丢(数据印证:用户第1轮01:53绑任务,第2轮02:48自由专注,间隔55分钟=中间关了重开)。
- **修复**:activeTaskId 挪进 store 持久化(落盘 data.json)。重启后恢复,选中的任务自然延续。schema v8→v9 迁移。
- **边界处理**:删除任务时若该任务正被选中→级联清空 activeTaskId(防悬空引用)。
- **新增取消选中**:再点一次已选中的任务=取消选中(回到自由专注)。子agent审查发现的体验缺陷,一并修复。

### 技术细节
- store.js 新增 setActiveTaskId/clearActiveTaskId;deleteTask 级联清空。
- App.jsx activeTaskId 改读 state.activeTaskId(不再 useState);onSelect 改调 store。
- TaskItem 点击逻辑:已选中再点→onSelect(null)取消。
- 172 测试绿(新增5个 activeTaskId 测试)。子agent自审通过(引用一致/ref模式成立/无悬空)。

---

## [v0.3.11] - 2026-06-26 (数据展示修复 + 图标排版修复)

> 🔧 修复"学了109分却显示25分"的困惑 + 消除图标换lucide后的"怪怪的"对齐问题。

### 修复·数据展示(自由专注统计盲区)
- **问题**:自由专注(没选任务)的学习时长,在"按科目统计"的展示处(今日计划面板/打卡表计划参考)被漏算,造成"学了109分但计划区显示25分"的困惑。根因不是统计bug(全局打卡判定/今日汇总都正确算全部),是按科目聚合的设计局限+文案误导。
- **修复**:
  - 今日计划面板标题加"今日总计 X"(含自由专注的全局值),每项明确标"本计划今日 Y"(区分全局vs该计划)
  - 打卡表计划参考标题加"今日共专注 X",每项文案从"今日已学"改"本计划今日"(消除和顶部"今日专注"的语义重叠)
- 子agent+小码审核确认无重复计算。

### 修复·图标排版(lucide SVG基线不齐)
- **问题**:emoji换lucide后,inline SVG默认baseline导致全站20+处图标相对文字"下沉",合起来就是用户说的"感觉怪怪的表达不出来"。
- **修复**:
  - 全局加 `svg.lucide { vertical-align: -0.125em }`(精准选择器,不影响燃尽图手写path)+ 各容器图标右margin 4px
  - 计时器按钮(.btn)、排序按钮(.task-item__sort-btn)、进度目标tab(.pf-goal-tab)改 flex 居中+合理热区
  - PartyPopper 庆祝图标配 success 色(原本灰色扫兴)
- 子agent审核确认无误伤、布局稳定。

### 技术细节
- 167 测试全绿(纯展示层改动,不动逻辑)。

---

## [v0.3.10] - 2026-06-26 (图标体系升级)

> 🎨 告别 emoji,全面采用专业图标库 lucide-react。

### 新增
- **lucide-react 图标库**:引入业界主流的线条图标库(MIT免商用)。图标用 currentColor 自动跟随主题配色,tree-shaking 后打包仅增 3KB。
- **全面替换 emoji**:11个组件文件、30+处 emoji 全部换成 lucide 图标:
  - 顶栏:🎯📋📈📊⚙️ → Target/CalendarCheck/TrendingUp/BarChart3/Settings
  - 计时器:🍅🌿⏸▶✓⏭↺ → Timer/Coffee/Pause/Play/Check/SkipForward/RotateCcw
  - 目标/科目/计划:🎯📚📦📋 → Target/BookOpen/Package/ListChecks
  - 任务操作:▲▼📍#× → ChevronUp/ChevronDown/MapPin/Hash/X
  - 连续打卡🔥→Flame,庆祝🎉→PartyPopper,燃尽图📉→TrendingDown 等
- **应用图标替换**:任务栏/桌面图标从 PIL 程序生成的纯色方块,换成专业矢量图标 A2(字节IconPark 线条番茄,Apache 2.0 免商用)。朱砂红着色,多尺寸 ICO(256/128/64/48/32/16)。用 sharp 渲染 SVG→PNG + PIL 合成 ICO。
- **保留 emoji 的场景**(规则):系统通知字符串(🍅☕)、状态文案字符串字段(✅⚠️❌)等无法嵌组件的位置。

### 技术细节
- 图标默认 size=16(行内)/18-20(标题按钮),连续打卡用 success 色。
- 应用图标生成流水线:iconify.design 下载 SVG → sharp 渲染 PNG(多尺寸) → PIL 合成 ICO。build/gen_icons.py 保留作备份(不再用)。
- 167 测试全绿(图标替换不影响逻辑层)。

---

## [v0.3.9] - 2026-06-25 (阶段4·进度预测)

> 📈 预测能不能按时完成:目标总进度 + 完成预测(双速率) + 燃尽图。

> **v0.3.9.1 补丁**(同日):今日计划面板显示"今日已学"。森哥反馈"只看到今日应学,看不到今日已学",新增 subjectTodayHours + todayPlanPanel显示今日已学(含完成度百分比)。子agent自审修2中危(多计划todayPct虚高 + CheckinPanel X/0h)。163测试绿。
> **v0.3.9.2 补丁**(同日):UI一致性打磨(小码整体审核7/10)。#1 SettingsPanel抽独立CSS+对齐其他弹层模板(固定宽/限高/header/×按钮/z-index20);#2 统一数字格式(fmtHours公共函数,影响4文件,告别3套混用)。167测试绿。#3-8待优化(记入档案)。
> **v0.3.9.3 补丁**(2026-06-26):完成UI一致性优化剩余6项(#3-8)。子agent自审抓出StatsPanel task-rank条漏改已补。167测试绿。
- #3 弹层宽度归档3档(简单480/标准640/复杂800),StatsPanel 600→640、GoalManager 720→800
- #4 z-index统一5个弹层overlay为50(原混用10/20/50)
- #5 进度条token(--bar-radius/--bar-track/--bar-transition),6处进度条引用统一圆角/动画/底色
- #6 ProgressPanel状态文案全emoji化(原no-data/no-deadline是破折号)
- #7 TodayPlanPanel每项meta从4行合并为2行(主=今日已学/应学+%,次=总计+剩余)
- #8 顶栏设置按钮前加分隔线(查看类与系统操作视觉分组)

### 新增
- **进度预测面板**:右上角新增 📈 按钮。打开后:
  - **目标总进度**:🎯目标进度条 + 各科目细分进度(按科目聚合,避免多计划算重)。
  - **完成预测**:每个进行中计划显示 截止日 vs 预测完成日 + 两个速率(近7天滚动 + 计划至今总均)。状态标注 ✅进度健康/⚠落后/—暂无数据。
  - **燃尽图**:SVG折线图。理想线(虚线,匀速完成)vs 实际线(实线,剩余工作量按天累计)。一眼看出跟上还是落后。
- **精度:科目级**(session无planId,按subject聚合,非计划级精确)。同科目多计划会近似叠加——够用,后续看是否需要计划级。

### 修复(小码审核发现)
- **[高危] 无计划科目虚高**:goalProgress把没计划的科目的学习也计入分子但分母为0,导致进度虚高+UI显示X/0h。已排除(无计划=还没规划,不计入完成度)。
- **[高危] 燃尽图实际线缺今天**:跨度>60天按周采样时,实际线最后一点最多在今天前6天,看着像"停了一周"。已补"今天点"。
- **[高危] predictFinishDate 时间不注入/不防remaining≤0**:破坏可测性+边界不健壮。已修(now可注入+已完成返回null)。

### 技术细节
- 新增 forecast.js 纯函数模块(subjectDoneSec/goalProgress/recentRate/overallRate/predictFinishDate/planForecast/burndownData)。
- goalProgress 按 subject 聚合 done(session按subjectId归集,天然去重),分母用该目标下各plan.totalHours求和。
- 燃尽图跨度>60天自动按周采样(stepDays=7)。
- 157 测试绿(新增31个:27个forecast + 4个审核回归)。

---

## [v0.3.8] - 2026-06-25 (阶段3·打卡表)

> 📋 看每天有没有坚持:日历热力图(一眼看出规律)+ 今日打卡清单 + 连续打卡天数。

> **v0.3.8.1 补丁**(同日):计划可编辑。森哥反馈"计划建好只能删不能改",给计划行加"改"按钮+行内编辑(改计划名/总时长/截止日),复用已有 updatePlan。校验失败时保存按钮禁用+提示。子agent自审通过。

### 新增
- **打卡表面板**:右上角新增 📋 按钮。打开后显示:
  - **连续打卡🔥N天**:从今天往回数连续达标的天数。
  - **今日打卡状态**:✅成功/⚠还差一点/❌还没开始,显示今日专注 vs 门槛。
  - **日历热力图**:月视图,每天一格,颜色=打卡状态(绿=达标/黄=部分/灰=缺勤/空=未到)。可左右切月,有"回到本月"。底部显示本月达标/部分/缺勤天数统计。
  - **今日计划进度(参考)**:列出各计划今日应学+完成进度条(只读,不影响打卡判定)。
- **每日专注门槛设置**:设置里新增"每日专注门槛"(默认30分钟)。每天专注达到这个时长算"打卡成功"。在⚙️设置→打卡里改。
- **打卡判定规则**:专注≥门槛=成功✅;专注=0=缺勤❌;0<专注<门槛=部分⚠;未来日期不判定。**用全局门槛,不绑计划目标**(简单清晰)。

### 重构
- **抽公共 date.js**:把散落在 stats.js 和 planning.js 的私有 dateKey/isValidFocus/isSameDay 收口成单一真相源(消除三套不一致实现的隐患)。两文件改用 import。
- **新增语义色 token**:success(翠绿)/warning(琥珀黄)/danger(朱红)+ soft 透明版,深浅通用,放 global.css + theme.js 镜像。

### 修复(小码审核发现)
- **[高危] 连续打卡中断语义 bug**:原逻辑"还没开始计数就继续往前找done"会导致"今天缺勤+昨天缺勤+前天达标"错误返回2(应返回0)。已修正:跳过今天后,遇到任何非done立即中断。补2个回归测试。

### 技术细节
- 新增 schema v7→v8 迁移(settings 补 dailyGoalMinutes,默认30),老用户已有值不被覆盖。
- 新增 checkin.js 纯函数模块(evaluateDay/calendarMonth/checkinStreak),便于单测。
- 周一起始日历(getDay 转换公式验证正确)。
- 130 测试绿(新增24个:20个checkin + 2个streak回归 + 2个v8迁移)。

---

## [v0.3.7] - 2026-06-25 (异常退出恢复)

> 🔄 软件闪退/电脑崩溃后,下次启动自动把"上次进行中但没正常结束的专注"按已专注时长记录,不再清零。

### 新增
- **异常退出恢复**:专注进行中遇到闪退/断电/强杀进程,下次启动检测到未完成的专注,自动按实际已专注时长记一条"异常中断"记录(计入今日统计和计划完成度)。主界面顶部显示恢复提示"🔄 上次专注未正常结束,已自动恢复 X 分钟"。
- **恢复判定规则**(防止垃圾记录):专注时长 ≥30秒 才恢复;<30秒 丢弃(防"点一下就关");时长限幅不超过设定总时长;开始时刻超过7天的脏数据丢弃。
- **暂停态安全处理**:暂停时清空快照,resume 时重写开始时刻——保证恢复时长只算最后一个连续 running 段,不会把暂停时长误计为专注(宁少算不多算)。

### 修复(小码审核发现)
- **[高危] 主界面 Timer 按钮绕过恢复逻辑**:开始/暂停/继续三个按钮原直接操作 timer 实例,没走 activeFocus 读写,导致最常用的主界面交互路径恢复功能完全失效。已统一委托 toggleStartPause。
- **[中危] 重置按钮不清快照**:用户点"重置"(语义=放弃)后若闪退,仍会误记一条。已清。
- **[中危] evaluateRecovery 防 totalSec 非法值**:NaN/负数会导致污染 sessions,已加守卫。

### 技术细节
- 新增 schema v6→v7 迁移(activeFocus 字段补 null),老用户数据自动升级。
- 新增 `activeFocus` 进行中快照({taskId, startedAtTs, totalSec}),随 data.json 落盘。
- 抽 `snapshotForTask` helper(快照逻辑单一真相源),addSession 和 recoverActiveFocus 共用。
- 新增 `evaluateRecovery` 纯函数(恢复判定),便于单测。
- stats.js / planning.js 的 isValidFocus 加"异常中断"状态(计入有效专注)。
- 107 测试绿(新增 13 个恢复逻辑测试)。

---

## [v0.3.6] - 2026-06-25 (目标计划系统·阶段2)

> 📋 给科目加"计划/轮次",设总时长+截止日,系统自动算每天该学多久。

### 新增
- **计划/轮次**:每个科目下可建多个计划(如"数学第1轮 50h 截止8.31")。在🎯目标面板点科目行的📋按钮展开计划区。
- **每日自动分配**:系统按 `(总时长-已完成)÷剩余天数` 算出"今天应学X小时"。某天没学自动往后分摊。
- **今日计划面板**:主界面新增"📋今日计划",汇总所有进行中计划,显示每个的应学时长+进度条+剩余天数/超期提示。
- **手动调整某天**:计划里可手动设某天学多久(覆盖自动分配)。
- 已完成时长自动算(专注该科目任务的记录归集,完成+提前结束都算)。

### 修复(小码审核)
- deleteGoal 漏删计划(悬空引用)→ 已修(级联删)。
- archiveSubject 没级联归档计划(归档科目后计划仍上今日面板)→ 已修(级联归档)。

### 设计决策(森哥确认)
- 已完成自动算/含周末/超期提示不强制/截止日含当天。

### 审核
- 小码审核通过(24轮,$0.39,1严重bug+7建议)。94测试绿。

---

## [v0.3.5] - 2026-06-24

### 修复(森哥反馈的严重BUG)
- **小窗口运行中卡住**:主窗口进后台时,Chromium对渲染层定时器节流,导致给小窗口发状态的500ms定时器停了 → 卡住。修复:两个窗口加 `backgroundThrottling:false`(B保险)。
- **计时结束不提醒**(更严重):主窗口后台时,渲染层250ms的"结束检测"定时器也被节流停了 → 时间到了也不提醒,打开主窗口才弹。修复:主进程接管结束检测(A根治)——渲染层上报"结束时刻",主进程用不受窗口影响的定时器监控,到点主动通知。

### 技术改进
- 计时结束检测改为"双源":渲染层(快)+主进程(后台可靠)。主进程只在渲染层timer还running时接管,防双触发重复记录。

### 审核
- 小码审核抓出2严重bug并修复:①handleFinishedRef忘了更新(主进程接管形同虚设)②双触发会重复记录session/重复通知(守卫方向错,reset后是stopped不是finished)。成本$0.41,13轮。76测试绿。

### 根因(沉淀)
Electron渲染进程定时器在窗口不可见时被Chromium节流(250ms→很久),这是多窗口应用靠渲染层定时器做核心计时的经典坑。解法:核心计时检测放主进程(不受窗口可见性影响)+ backgroundThrottling:false双保险。

---

## [v0.3.4] - 2026-06-24

### 修复(森哥反馈)
- **删除任务后排行榜显示"(已删除)"不好看**:给专注记录加 taskName 快照(存当时的任务名)。现在删了任务,排行榜显示原名(如"高数第三章")而非"(已删除)"。
- 与 v0.3.3 的科目快照同设计(schemaVersion v4→v5,迁移回填老数据)。

### 设计决策(森哥确认)
- 任务**改名**后(没删),排行榜历史显示**旧名**(历史保真,和科目快照一致)。即1月叫"高数"、2月改名"高等数学",历史记录仍显示"高数"。

### ⚠️ 诚实告知(历史局限)
- v0.3.4 **之前**删掉的测试任务,其历史记录的 taskName 无法回填(任务已删,无快照源),仍显示"(已删除)"。本次只保证**以后新产生的**专注记录有 taskName。建议森哥把那几条旧测试数据手动清掉(或保留作纪念)。

### 审核
- 子agent自我审核通过(简单迭代,与科目快照同构)。76测试全绿。

---

## [v0.3.3] - 2026-06-24 (结构重构·不改功能)

> 🔧 按体检报告做结构梳理。**功能/界面完全不变**,只整理代码,为阶段2筑牢地基。

### 重构(P0 必做 + P1/P2 选择性)
- **session 存科目快照(P0-1)**:专注记录现在存"当时的科目名/目标名",阶段4统计时科目改名/删除,历史仍准确。
- **存盘加版本号(P0-3)**:数据文件有 schemaVersion,加功能改结构按"版本链"顺序迁移,告别"猜字段"。
- **分组逻辑加测试收口(P0-2)**:新增 features/grouping.js(groupTasks/可见任务/活跃科目),补13个测试。这是之前最易出bug却没测试的地方,现在护住了。
- **时长格式收口(P1-6)**:新增 features/format.js,四个组件统一用。
- **清理(P2-7/8)**:SettingsPanel CSS复用加注释;快捷键死代码消除。
- **行为差异(可接受)**:今日小计时长从"X分钟"统一为"X分/X时X分"(和统计面板一致)。

### 评估后暂缓(记录,非不做)
- 拆 App.jsx(P1-4):风险>收益,App虽挤但逻辑正确,强行抽hook可能引入时序bug。等真撑不住再拆。
- 魔法数字集中(P2-9)、确认弹窗换组件(P2-10):收益低,重构期不引入新风险。

### 审核
- 小码审核因 GLM 529 过载3次失败 → 子agent自我审核,**行为等价无严重问题**(子agent读了旧构建产物逐字符对比确认)。71测试全绿(新增format/grouping/迁移20个)。

### 体检背景
森哥(非开发者)提出"功能多了怕bug修复不过来",触发全面代码体检。体检发现核心问题是"测试防护墙被新功能绕过",本次把墙补好了。

---

## [v0.3.2] - 2026-06-24

### 新增
- **📦 归档区**:目标管理面板加"归档区"tab,集中查看所有已归档的目标/科目,可**恢复**(取消归档,任务因归属还在自动重新显示)或**彻底删除**。归档不再是"藏起来就找不回",闭环了。
- 归档目标时,其下科目标"随目标恢复"(级联);未归档目标下单独归档的科目单独列出。
- 归档区 tab 徽标显示归档总数(归档目标+单独归档科目),单独归档科目也能被提醒到。

### 设计
- **恢复与归档对称**:unarchiveGoal 连带恢复其下科目(镜像 archiveGoal 的级联归档);unarchiveSubject 只恢复自己。
- **数据完整性**:归档/恢复全程不碰 task.subjectId,任务归属关系永远在,历史统计不丢。

### 审核
- 小码审核通过(14轮,成本$0.39,无严重问题)。修1条建议(徽标计数含单独归档科目)。51测试绿。

---

## [v0.3.1] - 2026-06-24

### 修复(森哥体验反馈)
- **删科目后无法输入新科目**:GoalManager 的 selectedGoalId 失效未自动回退,导致输入框因渲染分支闪烁而失焦。加 useEffect 同步,失效自动回退到第一个目标。
- **归档不散落**:归档目标/科目后,其下任务不再散落到"自由任务"组,而是从主视图**隐藏**(归属关系保留,数据不丢)。归档 vs 删除语义清晰化:归档=藏起来保留归属,删=清归属变自由任务。
- **归档科目任务的视觉一致性**:📍按钮对归属已归档科目的任务,显示归属名+(已归档)灰色标记;标签筛选栏不再出现"归档任务的标签却点进去无任务"的幽灵计数。

### 新增(森哥建议)
- **科目排序**:目标面板里科目可 ▲▼ 上下排序(同目标内),左侧分组顺序跟随。和任务排序一样的逻辑(normalize+迁移)。

### 审核
- 小码审核因 GLM 网关 529 过载连续3次失败 → 降级子agent自我审核。**无严重问题**,修了2条建议(归档高亮/标签幽灵计数)。48测试绿。
- 注:子agent专项分析确认 useEffect 不会死循环(自洽终止)。

---

## [v0.3.0] - 2026-06-24 (目标计划系统·阶段1)

> 🎯 番茄钟从"计时工具"升级为"目标驱动的学习管理系统"。本版为阶段1:目标/科目/任务归属。

### 新增
- **三层结构:目标 → 科目 → 任务**。可建目标(如"考研",含截止日)、在目标下建科目(如"数学/英语/专业课")。
- **任务归属科目**:每个任务能归属到一个科目(点任务上 📍 按钮选择);无归属的归"自由任务"。
- **左侧按科目分组显示**:任务按所属科目分组(📚科目组 + 💼自由任务组),每组可折叠,组标题显示任务数。
- **🎯 目标管理面板**:右上角🎯按钮打开,管理目标(增删改/归档/截止日)和科目(增删改/归档)。

### 设计要点
- **session 结构不变**:经 task.subjectId 间接归属科目(session→task→subject→goal),老数据零迁移。
- **级联规则**:删目标连带删其下科目(任务变自由任务保留);归档目标连带归档科目;删科目任务变自由任务。任务永不被删。

### 审核
- 小码审核通过(6轮,成本$0.34,无严重问题)。修复2条建议:归档后目标详情面板收口、React select 归档边界告警。

### 待办(阶段2+)
- 阶段2:计划/轮次(总时间+截止日)+每日自动分配
- 阶段3:番茄钟计时→打卡表
- 阶段4:进度条+燃尽图+完成预测
- 小码遗留建议:moveTask组内/全局口径文档化、session的subjectId快照策略(阶段2统计前定)

---

## [v0.2.6] - 2026-06-23

### 新增
- **待办任务排序**:每个任务右侧加 ▲▼ 上下箭头按钮,点击调整任务在清单里的顺序(悬停时显示)。排序持久化,关窗重开顺序不变。

### 修复(小码审核发现2严重bug)
- **order 兜底值三处不一致导致旧任务和新任务塌到同一 order**:统一为 `?? 0`,并在加载持久化数据时做一次性迁移(无 order 的旧任务按 createdAt 补 0..n-1)。
- **order 相等时排序按钮点了没反应**:moveTask 后做 normalize(所有任务 order 重写为连续 0..n-1),消除空洞和相等。
- **筛选标签时排序会错位**:filterTag 激活时隐藏排序按钮(子集排序会与全局 order 相邻关系不一致)。

### 审核
- 小码审核通过(6轮,成本$0.16),发现并修复上述2个严重bug(都是"点了没反应"的用户体验杀手)。

---

## [v0.2.5] - 2026-06-23

### 修复
- **月度柱状图对齐错位**(森哥反馈):柱子和日期标签没对齐。根因:.chart__col 用 flex 等宽分列,但标签文字宽度不一("1号"窄/"10号"宽),密集模式下宽标签撑宽所在列打破等宽。修复:.chart__col 改 `flex:1 1 0; min-width:0` 严格等分;标签 `.chart__day` 加 overflow/nowrap 防溢出。
- 月视图标签简化为纯数字(去掉"号"字),更窄不截断。

### 审核
- 子agent自我审核通过(简单迭代)。确认对齐修复有效,周/年视图不受影响,无误伤。

---

## [v0.2.4] - 2026-06-23

### 新增
- **统计扩展到月/年**:趋势图加"近7天/本月/本年"切换 tab。月视图按天显示(1号~今天),年视图按月显示(1-12月,只统计本年)。
- 概览卡片的"累计"随范围切换(本周/本月/本年累计)。

### 优化
- **统计口径修正**:新增 `isValidFocus`,统一判定"专注且 完成/提前结束 才算有效专注"(跳过不算)。summarizeToday/topTasks/dailyTrend/monthlyTrend/yearlyTrend 全部改用,口径一致。这样 v0.2.3 的"完成本轮(提前结束)"数据能正确进入统计。
- 月视图 label 改"X号"(避免纯数字歧义)、隔5显示避免重叠;月视图柱子缩窄(chart--dense)。
- store.js session 的 status 注释更新为实际三种值。

### 审核
- 小码审核通过(14轮,成本$0.25,无严重问题)。审核中修正了 max-turns(10→15,读多文件+分析需要更多轮)。

---

## [v0.2.3] - 2026-06-23

### 修复(森哥反馈的BUG)
- **小窗口卡顿BUG**:计时到最后阶段/切休息时,小插件倒计时会"卡住",必须打开主窗口才恢复。根因:上报 interval 依赖数组含 mode/task,变化时 interval 被销毁重建,新 interval 首次触发要等500ms导致数据冻住。修复:interval 改只依赖 `[]` 全程不重建,用 ref 读最新值。

### 新增
- **✓ 完成本轮**(中途结束按实际时间记录):设定25分钟,20分钟临时有事可直接点"完成本轮",按20分钟记为"提前结束"并进入休息(不再因没满25分钟被清零)。区别于"跳过"(记跳过)。

### 优化(小码审核建议)
- 新增"提前结束"状态,与"完成"(自然到点)区分,供统计区分。
- 0秒边界:还没专注就点"完成本轮"时自动走跳过逻辑。
- API 就绪重试:reportTimerState 的 api 判断挪进 interval。
- Timer 的 lastStatus 改用 useRef。按钮文案/tooltip 更直白。

### 审核
- 小码审核通过(2分58秒,成本$0.17,无严重问题)。

---

## [v0.2.2] - 2026-06-23

### 修复
- **单实例锁严重 bug**(小码审核发现):`app.quit()` 是异步的,第二个实例退出前会继续执行后续代码,导致重复创建窗口、抢注全局快捷键、重复注册 IPC handler。已用 `else` 块包裹所有初始化逻辑修复。
- 重复启动时小插件窗口一并唤起(之前只唤起主窗口)。

### 流程
- 本次审核用新完整命令(`--bare --max-turns 10 --max-budget-usd 0.5 --allowedTools Read --output-format json`)成功跑通,3分12秒,成本$0.26,未超时。验证了调用命令修复有效。

---

## [v0.2.1] - 2026-06-23

### 新增
- **单实例锁**:只能打开一个番茄钟。重复点击桌面图标时,不会开第二个,而是自动把后台已有的番茄钟**唤起到前台**。

### 技术改进
- 用 Electron `requestSingleInstanceLock` 实现单实例,`second-instance` 事件唤起主窗口。
- ⚠️ 此版本单实例实现有 bug(见 v0.2.2 修复),已过时。

---

## [v0.2.0] - 2026-06-23

### 新增
- **任务标签系统**:每个任务可打多个标签;左侧栏支持按标签筛选任务。
- **浅色/深色主题**:跟随 Windows 系统设置自动切换(无需手动调)。
- **全局快捷键**:`Ctrl+Shift+Space` 开始/暂停,主窗口不在前台也能用。
- **小插件浮窗**:极简独立窗口(倒计时数字 + 开始/暂停按钮),常驻置顶可拖动。托盘菜单可显示/隐藏。
- **专注/休息结束通知**:完成时弹出系统通知提醒。

### 体验改进
- 设计审美套用 frontend-design 技能:番茄朱砂红 + 暖墨底,oklch 配色,Outfit×思源黑体。
- 计时数字微光晕、按钮悬浮位移、设置弹层毛玻璃模糊等细节。

### 修复
- (审核发现的 3 个高危 bug)跳过后计时器卡死;异步加载覆盖用户数据;skip 没清零累计值。

---

## [v0.1.0] - 2026-06-23 (首发)

### 核心功能
- **可自定义时长的番茄钟**:工作/休息时长可在设置里改(默认 25/5 分钟,范围 1-120 分钟),不锁死。
- **任务绑定的待办清单**:选任务→点开始→专注时长自动归属该任务。
- **自动记录**:每次专注生成一条记录(任务/时长/开始时间/状态)。
- **本地持久化**:数据存本机,关窗重开还在。
- **今日小计 + 完整统计面板**:今日专注时长/番茄数 + 近 7 天趋势柱状图 + 任务时间去向排行。
- **系统托盘**:关闭最小化到后台,双击托盘恢复。
- **开机自启**:设置面板开关控制。
- **移除默认英文菜单栏**:窗口更干净。

### 技术栈
- Electron 31 + React 18 + Vite
- 计时核心用时间戳差值法(系统休眠唤醒后时间仍准)
- 界面与样式分离(token 化,换主题只改 styles 层)
- TDD:19 个单元测试

### 交付方式
- 免安装版(portable),手动组装(electron-builder 在国内网络不可靠,见第二大脑经验)。
- 桌面快捷方式 + 托盘常驻。

---

## 未发布的功能规划(后续考虑)

- 🔔 专注结束时声音提醒(目前仅系统通知)
- 🎵 专注时白噪音
- 云同步(手机端可见)
- 正规安装包(electron-builder 打通后)
